{"id":"sML2JBPsdAyCMAUuDAb4rMUzpB9GRgBrSJo6frY3aqUzYMyY1KqfA5MTDk9Bkxae","title":"Blog - Bitfield Consulting","displayTitle":"Dev - Bitfield","url":"https://bitfieldconsulting.com/posts?format=rss","feedLink":"https://bitfieldconsulting.com/posts/","isQuery":false,"isEmpty":false,"isHidden":false,"itemCount":1,"items":[{"title":"Things fall apart","url":"https://bitfieldconsulting.com/posts/things-fall-apart","date":1742815260,"author":"John Arundel","guid":126,"unread":true,"content":"<p>Everything that can go wrong  go wrong, as we all know,\nand that goes double for programmers. With software, everything probably\nalready  gone wrong, but the tests haven’t detected it,\nbecause the tests are broken too.</p><p>Never mind. We do what we can, and one thing we can do is ensure that\nour programs  things to go wrong all the time, and\nrecover gracefully when they do—or, at least, report the problem in a\nhelpful way.</p><p>In <a href=\"https://bitfieldconsulting.com/posts/magic-function\">The magic function</a>, we started\nbuilding a little line-counting program in Rust, like the Unix\n tool. We imagined some magic function\n that would do the hard work for us, and we\nwrote a simple test for it:</p><pre><code></code></pre><p>By <a href=\"https://bitfieldconsulting.com/posts/writing-terrible-code\">writing terrible code</a>,\nwe checked that this really tests something, because we now know it\nfails when  returns the wrong answer. Then we\ngot the test passing, with the following function that really does count\nlines:</p><pre><code></code></pre><p>So, do we have a production-ready line counter crate yet? Well, not\nquite, because in real life (as opposed to programming tutorials) things\nalways seem to go wrong somehow, don’t they?</p><p>In my book <a href=\"https://bitfieldconsulting.com/books/rust-tools\">The Secrets of Rust:\nTools</a>, we’ll see that programs can fail in all sorts of ways. Some\nof them are due to —where the programmer has done something\nwrong. Others are just due to things going wrong in the environment\nwhere the program is running.</p><p>These  errors are predictable, in the sense that we\nusually know  they can occur, even if we don’t know\nexactly  they’ll occur. For example, if we’re trying to\nopen some file, the file might not exist, or we might not have\npermission to read it. If we’re trying to write to a file, there might\nbe no disk space left. And so on.</p><p>As we saw in <a href=\"https://bitfieldconsulting.com/posts/rust-errors-option-result\">Rust error\nhandling is perfect actually</a>, the  type conveys a\ndata value that might exist, or not, because maybe some error happened.\nA  can be one of two variants: either\n, meaning that we have the value , or\n, meaning some error .</p><p>Any time some function returns a , you know that\nsome kind of error could happen, so it’s wise to be prepared for it.</p><p>So, what about the  trait? Can there be an error\nreading from a reader? Let’s have a look:</p><pre><code></code></pre><p>Well, of  there can—in our secret hearts, we knew that\nanyway. Reading a byte stream from any external source can fail for all\nsorts of reasons. But the paperwork confirms it: \nreturns a .</p><p>What this means is that if the result is the  variant,\nthen it will contain a : the number of bytes\nsuccessfully read on this call. But if it’s the \nvariant, the error will tell us what went wrong.</p><p>We don’t call  directly in our\n function, but we call  on\n, using the  trait, and\n will call  to get the data. So you might\nbe wondering what happens if there  some error from the\nunderlying reader. What does  do in that case?</p><p>There are lots of things it  do: for example, it could\npanic, or stop iterating, or yield an empty line but keep going, and so\non. None of these seem quite satisfactory. What it \ndoes is, I think, exactly right. It doesn’t make any decision at all,\nbut passes the decision on to us, by yielding a \nitself:</p><pre><code></code></pre><p>In other words, a  is not an iterator of\n, but of line . That is, each item yielded\nby  is a  that either contains the\nline, or an error explaining why not.</p><h3>Does our program handle\nerrors?</h3><p>So what does  code do when that  is\n? Let’s take another look:</p><pre><code></code></pre><p>Well, “nothing special” is the answer! We just count all the items\nyielded by , whether they’re  or\n.  doesn’t seem right, does it?</p><p>So if there  an error reading the input, it sounds like\nour program would sit there not printing anything, and never getting\nanywhere. So, can we test this out? The program reads from standard\ninput, so how could we deliberately trigger an error there?</p><p>One way would be to use the shell to  something into the\nprogram’s standard input. For example, we can pipe the contents of a\nfile, so that the program counts the lines in the file:</p><p>Fine, that works, but what we want is something that \nbe read this way, so we can see what the program does on encountering a\nread error. Well, depending on your operating system, you usually can’t\nread a directory. So attempting to pipe in “”, the\nshorthand for the current directory, probably won’t work. Let’s try it\nwith :</p><pre><code>wc: stdin: read: Is a directory</code></pre><p>Exactly! That’s what  saying. So what does our Rust\nprogram do in the same situation? Time to find out:</p><p>Huh. It’s stuck, which is what we predicted. That’s no good, because\nusers can’t tell if there’s really a problem, or the program’s just\ntaking a while, or is waiting for more input. We should be able to\ndetect the error, print some informative message about it, and stop,\njust like  does.</p><p>Congratulations! We’ve found a bug. Good programmers are always\ndelighted when they find a bug—not because they like bugs, of course,\nbut  one means there’s now a chance of fixing it. So how\nshould we go about this?</p><p>We need to modify the  function to do\nsomething different if it encounters a read error, but there’s something\neven more important we need to do before that. We need to add a\n that proves  handles errors\ncorrectly.</p><p>What’s the point of that, when we already know it doesn’t? Well, that\n the point. We knew we’d correctly implemented the “counts\nlines” behaviour when the “counts lines” test stopped failing and\nstarted passing. Similarly, we’ll know we’ve correctly fixed the\n“handles errors” bug when the “handles errors” test stops failing and\nstarts passing.</p><p>To test this, we want to call  with something\nthat implements , but always returns an error. That’s\neasy to write:</p><pre><code></code></pre><p>Right? All you need in order to be a reader is to implement the\n method, and it returns . So we\ndon’t even need to do any actual reading: we can just return the\n variant directly, giving it some \nmade out of a string (“oh no”).</p><p>This is useless as a , of course: it can’t read\nanything. But it’s awfully useful for our test, because as soon as the\n iterator tries to read from it, it will blow up, and\nour test can check what happens.</p><p>To make our useless reader acceptable to ,\nthough, we need to wrap it in a , so let’s first\nimport that:</p><p>And here’s how we use it:</p><pre><code></code></pre><p>If  returns anything other than\n, this test will fail. As a quick thought experiment,\nwhat will happen if we run this test now? Will it fail?</p><p>Well, no, because it doesn’t actually compile yet:</p><pre><code>no method named `is_err` found for type `usize`</code></pre><p>That’s fair. In order to even see the test , we need to\nmodify  to return a  rather\nthan a plain old . Let’s do that:</p><pre><code></code></pre><p>Notice that once we update the function’s return type to\n, we also have to wrap the\n iterator expression in . That’s\nnot inferred: we have to explicitly say that the value we’re returning\nis , not .</p><p>Now the test compiles, but if our guess is right, it still won’t\nfail, because  just won’t ever return. Let’s\nsee:</p><p>All right, then. We’ve  the bug in a test; in the\nnext post, we’ll see if we can figure out how to fix it. Until then,\n, and happy Rusting.</p>","contentLength":6935,"flags":null,"enclosureUrl":"","enclosureMime":"","commentsUrl":null}],"tags":["dev"]}