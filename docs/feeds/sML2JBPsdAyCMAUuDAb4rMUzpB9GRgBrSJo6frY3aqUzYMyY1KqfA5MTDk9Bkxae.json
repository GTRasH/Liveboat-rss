{"id":"sML2JBPsdAyCMAUuDAb4rMUzpB9GRgBrSJo6frY3aqUzYMyY1KqfA5MTDk9Bkxae","title":"Blog - Bitfield Consulting","displayTitle":"Dev - Bitfield","url":"https://bitfieldconsulting.com/posts?format=rss","feedLink":"https://bitfieldconsulting.com/posts/","isQuery":false,"isEmpty":false,"isHidden":false,"itemCount":2,"items":[{"title":"Here comes the sun","url":"https://bitfieldconsulting.com/posts/here-comes-sun","date":1751790480,"author":"John Arundel","guid":135,"unread":true,"content":"<p>We’re better together, and that’s as true of software engineers as it\nwas of the Beatles. None of us is as smart as all of us, and this is why\nin my book <a href=\"https://bitfieldconsulting.com/books/rust-tools\">The Secrets of Rust: Tools</a>, I\nencourage thinking about every program we write as a kind of global\ncollaboration. By designing our Rust programs as modular, reusable\ncomponents——and publishing them to the universal library,\nwe make it possible for others to connect our software with their\nown.</p><p>The results of these collaborative efforts are better than any of us\ncould have achieved by ourselves, and the same principle applies to our\nprograms, too: when they connect with each other, they can do great\nthings. So one of the most important skills in software engineering is\nbeing able to write programs that talk to other programs, often over\nnetwork connections and using protocols like HTTP and JSON.</p><p>For example, let’s think about a Rust program that gets the current\nweather conditions for your location (or any location). There are\nseveral suitable public APIs for this, and we don’t need anything fancy,\nso we’ll start with <a href=\"https://weatherstack.com/\">weatherstack.com</a>.</p><p>Most services like this require some kind of API , or\nauthentication token, either for billing or just to limit usage.\nWeatherstack is no exception, so in order to use it we’ll first need to\nsign up for at least a <a href=\"https://weatherstack.com/signup\">free\naccount</a>.</p><p>The free plan limits us to 100 requests a month, but that’s okay: we\nprobably won’t be needing to check the weather all that often in\npractice. We’ll just have to be a little bit careful while developing\nthe tool that we don’t accidentally use up all our quota, but I don’t\nthink it’ll be a problem.</p><p>Once we’re signed up, we can get our access key, which will look\nsomething like this:</p><pre><code></code></pre><p>This is essentially our password to the Weatherstack service, so\nwe’ll want to keep it secret, and we also don’t want to embed it in our\nprogram. Instead, we’ll need to provide a way for users to supply their\n API key when they run the program (for example, in an\nenvironment variable).</p><h2>Making HTTP requests with\n</h2><p>Let’s start by writing the simplest imaginable weather tool, then:\none that makes an HTTP request to the Weatherstack API, using a suitable\nkey, and prints the results.</p><p>As usual, there’s a crate that makes this sort of thing pretty easy:\nit’s called  (yes, with a “w”). Let’s start a new\nproject and add  as a dependency:</p><p><strong><code>cargo add reqwest -F blocking</code></strong></p><p>Don’t worry about what the  feature does for the\nmoment. Let’s get a sketch version working first.</p><p>Here’s our first attempt:</p><pre><code></code></pre><p>We use  to get the API key from an environment\nvariable (its name is up to us, but this seems like a reasonable\nchoice).</p><p>Next, we use  to create a new\n client, which is the thing we’ll use to make the\nHTTP request. To do that, we call the client’s  method\nwith the Weatherstack URL. To this we add a couple of URL parameters:\n (the location we want the weather for), and, of\ncourse, , so that we can pass our API key.</p><p>Calling  actually makes the request, and, since\nthat could fail, we need to  the result to get the\nHTTP response, which we’ll call . And we’ll just print\nwhatever the response body contains for now, using\n.</p><p>You may be fuming with righteous anger by now, thinking “But this\ncode is terrible! It’s not an importable crate, it doesn’t handle errors\nproperly, and it doesn’t let users specify their location!”</p><p>And you fume some good points, dear reader. Do register your\ncomplaints on Hacker News by all means (“”),\nbut let me say one thing. In <a href=\"https://bitfieldconsulting.com/books/rust-tools\">The Secrets of\nRust: Tools</a>, one of the insider secrets I reveal is that it’s okay\nto <a href=\"https://bitfieldconsulting.com/posts/writing-terrible-code\">write terrible code</a>. At\nleast, at first. In fact, it’s a great idea to deliberately write a\nsketch version of your program that you  you’re going to\nthrow away and replace.</p><p>By doing this, you can try out the program to make sure it actually\ndoes what you want, which is kind of important. If so, the sketch\nversion will start to point the way to what <a href=\"https://bitfieldconsulting.com/posts/magic-function\">magic functions</a>—what\n—you might include in a properly-structured\nversion. And, since many of our initial decisions about this will be\nwrong, a preliminary sketch will help us avoid getting locked into\nthem.</p><p>So let’s see if our sketch version of the weather client will do\nanything useful at all. Nope:</p><pre><code>...\ncalled `Result::unwrap()` on an `Err` value: NotPresent\n...</code></pre><p>We’re calling  to get the API key, and that’s\nreturning . Well, of course: we haven’t set that\nenvironment variable yet. Let’s do that (replace my dummy value with\nyour own real key):</p><p><strong><code>export WEATHERSTACK_API_KEY=f4ac3e4c75d34cc2be60b0628e7b2ecc</code></strong></p><pre><code></code></pre><p>This looks promising. If you get something that looks like an error\nresponse instead, check that you used the right API key and that it’s\nactivated (sometimes it takes a little while for a new key to start\nworking).</p><p>Let’s copy this JSON output and save it in a file, because it’ll come\nin useful for testing later on. For now, though, we can just have a look\nat it and see if it contains the two bits of information we actually\nwant: the temperature and the description.</p><pre><code>\"temperature\": 11.2,\n...\n\"weather_descriptions\": [\"Sunny\"],</code></pre><p>Looks like it’s currently 11.2ºC and sunny in London. For my American\nfriends, that’s about fifty degrees Fahrenheit (and don’t worry, no\ndisrespect is intended by giving the temperature only in Celsius: more\non that later).</p><p>We have a working, minimal weather client. Yes, the presentation\nneeds a bit of work, and it might be useful to get the weather for\nlocations other than London, but that’s all detail. The point is that we\nhave something that runs and prints the weather, sort of.</p><p>Now we can start to turn it into a proper, grown-up crate, with\ntests, a nice user interface, and so on. Let’s start, as usual, by using\nthe “magic function” approach to design the API of the crate. What’s the\nleast we could write in ? This time, you can try coming\nup with the initial sketch.</p><p> Write the  function for the\nreal weather tool, using magic functions where necessary. Users should\nbe able to supply their chosen location as a command-line argument.</p><p> Well, you already know how to get the API key\nfrom an environment variable, and how to get the program’s command-line\narguments. What we  want to do in  is\nactually make the HTTP request: that’s too fiddly and low-level. That’s\na good candidate for pushing down into a magic function. But what would\nit need to take? What would it return? See what you can do.</p><p> Here’s my version:</p><pre><code></code></pre><h2>Just like that: a\nmagic  function</h2><p>This isn’t the final version, of course, but it’ll do to get us\nstarted. We now know what the magic function needs to be:</p><pre><code></code></pre><p>What we  is the location and key; what we \nis the weather, so we invent exactly the magic function that turns one\ninto the other. </p><p>A good abstraction hides the implementation details that the calling\nfunction doesn’t need to know about. In this example, \ndoesn’t have to know anything about HTTP, or even about\nWeatherstack.</p><p>Indeed, it doesn’t even know what type of value\n returns. We can infer it’s a\n of something, but we don’t know what. And we don’t\nneed to know! We just print it, straightforwardly:</p><p>We’re saying that whatever type this is, it has to implement\n, so that it knows how to turn itself into a string\nrepresenting the weather conditions.</p><p>Fine. Let’s see if we can build this magic \nfunction, then. As usual, we’ll start with a test in\n:</p><pre><code></code></pre><p>This  reasonable, and indeed it is. But it doesn’t\ncompile yet, because it refers to some magic struct \nthat hasn’t yet been defined. That’s easily fixed, though: we’ll define\nit.</p><pre><code></code></pre><p>We derived  and  so that we\ncan compare values using , as we’d like to in the\ntest.</p><p>We’ll store the temperature as an  (Rust’s 64-bit\nfloating-point type, which can store fractional numbers). There’s more\nwe could do here, as we’ll see later, but an  will do\nfor now.</p><p>And we’ll need a null implementation of  to\ntest against:</p><pre><code></code></pre><p>This is fine, but Rust complains that we haven’t yet implemented\n for , as we promised, so let’s\nfake it for now:</p><pre><code></code></pre><p>A bit cheeky, but we know this isn’t the final version, just\nscaffolding. In effect, we’re implementing  by using\nthe  implementation we already derived.</p><p>Now we can run the test, and, as expected, it fails:</p><pre><code>assertion `left == right` failed: wrong weather\n  left: Weather { temperature: 11.2, summary: \"Rainy\" }\n right: Weather { temperature: 11.2, summary: \"Sunny\" }</code></pre><p>Great. That validates the test, so in the next post we’ll go ahead\nand write the real version of .</p><p> Can you spot at least one potential problem with\nthis test? Here it is again:</p><pre><code></code></pre><p>Tune in next time to see if you were right!</p>","contentLength":8624,"flags":null,"enclosureUrl":"","enclosureMime":"","commentsUrl":null},{"title":"The joy of (type) sets","url":"https://bitfieldconsulting.com/posts/type-sets","date":1749922260,"author":"John Arundel","guid":134,"unread":true,"content":"<blockquote><p><em>A set is a Many that allows itself to be thought of as a\nOne.</em>\n—Georg Cantor, quoted in Rudy Rucker’s <a href=\"https://amzn.to/3viu3v8\">“Infinity and the Mind”</a></p></blockquote><p>Let’s talk about sets, baby. The point of <a href=\"https://bitfieldconsulting.com/posts/generics\">generic programming</a>, as we’ve learned in this\nseries, is to be able to write code that operates on more than one\nconcrete data type. That way, we don’t have to repeat the same code over\nand over, once for each kind of data that we need it to handle.</p><p>But being free and easy about your data types can go too far: <a href=\"https://bitfieldconsulting.com/posts/type-parameters\">type parameters</a> that accept literally\n kind of data aren’t that useful. We need <a href=\"https://bitfieldconsulting.com/posts/constraints\">constraints</a> to reduce the set of types\nthat a function can deal with. When the type set is infinite (as it is\nwith ], for example), then there’s almost nothing we\ncan do with those values, because we’re infinitely ignorant about\nthem.</p><p>So, how can we write more flexible constraints, whose type sets are\nbroad enough to be useful, but narrow enough to be usable?</p><p>We already know that one way an interface can specify an allowed\nrange of types is by listing , such as\n. We’ll use the term  to describe interfaces like these that contain only\nmethod elements, but now let’s introduce another kind of interface.\nInstead of listing methods that the type must have, it directly\nspecifies the set of types that are allowed.</p><p>For example, suppose we wanted to write some generic function\n that multiplies a number by two, and we want a type\nconstraint that allows only values of type . We know\nthat  has no methods, so we can’t use any basic\ninterface as a constraint. How can we write it, then?</p><pre><code></code></pre><p>Very straightforward! It looks just like a regular interface\ndefinition, except that instead of method elements, it contains a single\n, consisting of a named type. In this case, the\nnamed type is .</p><h2>Using a type set constraint</h2><p>How would we use a constraint like this? Let’s write\n, then:</p><pre><code></code></pre><p>In other words, for some T that satisfies the constraint\n,  takes a T parameter and\nreturns a T result.</p><p>Note that we now have one answer to the sort of problem we\nencountered when trying to write an  function:\nhow to enable the  operator (or any other arithmetic\noperator) in a parameterised function. Since T can only be\n (thanks to the  constraint), Go can\nguarantee that the  operator will work with T values.</p><p>It’s not the complete answer, though, since there are other types\nthat support  that  be allowed by this\nconstraint. And in any case, if we were only going to support\n, we could have just written an ordinary function that\ntook an  parameter.</p><p>So we’ll need to be able to expand the range of types allowed by our\nconstraint a little, but not beyond the types that support\n. How can we do that?</p><p>What types  satisfy the constraint ?\nWell, only ! To broaden this range, we can create a\nconstraint specifying more than one named type:</p><pre><code></code></pre><p>The types are separated by the pipe character, . You\ncan think of this as representing “or”. In other words, a type will\nsatisfy this constraint if it is … you get the idea.</p><p>This kind of interface element is called a . The type\nelements in a union can include any Go types, including interface\ntypes.</p><p>It can even include other constraints. In other words, we can\n new constraints from existing ones, like this:</p><pre><code></code></pre><p>We’re saying that , , and\n are all unions of different built-in numeric types,\nbut we’re also creating a new constraint , which is a\nunion of those three  types we just defined. If it’s\nan integer, a float, or a complex number, then it’s a number!</p><h2>The set of all allowed types</h2><p>The  of a constraint is the set of all types that\nsatisfy it. The type set of the empty interface () is\nthe set of all types, as you’d expect.</p><p>The type set of a union element (such as  in the\nprevious example) is the union of the type sets of all its terms.</p><p>In the  example, which is the union of\n, its type set contains\n, , and no other types.</p><p>You probably know that with a basic interface, a type must have\n of the methods listed in order to implement the interface.\nAnd if the interface contains other interfaces, a type must implement\n of those interfaces, not just one of them.</p><pre><code></code></pre><p>If we were to write this as an , we would\nseparate the methods with a semicolon instead of a newline, but the\nmeaning is the same:</p><pre><code></code></pre><p>To implement this interface, a type has to implement . Just one\nor the other isn’t good enough.</p><p>Each line of an interface definition like this, then, is treated as a\ndistinct type element. The type set of the interface as a whole is the\n of the type sets of all its elements. That is,\nonly those types that all the elements have in common.</p><p>So putting interface elements on different lines has the effect of\nrequiring a type to implement  those elements. We don’t need\nthis kind of interface very often, but we can imagine cases where it\nmight be necessary.</p><p>You might be wondering about what happens if we define an interface\nwhose type set is completely empty. That is, if there are no types that\ncan satisfy the constraint.</p><p>Well, that could happen with an intersection of two type sets that\nhave  elements in common. For example:</p><pre><code></code></pre><p>Clearly no type can be both  and \nat the same time! Or, to put it another way, this interface’s type set\nis empty.</p><p>If we try to instantiate a function constrained by\n, we’ll find, naturally enough, that it can’t be\ndone:</p><pre><code>cannot implement Unpossible (empty type set)</code></pre><p>We probably wouldn’t do this on purpose, since an unsatisfiable\nconstraint doesn’t seem that useful. But with more sophisticated\ninterfaces, we might accidentally reduce the allowed type set to zero,\nand it’s helpful to know what this error message means so that we can\nfix the problem.</p><p>A  type is one that’s built up from other types. We\nsaw some composite types in the <a href=\"https://bitfieldconsulting.com/posts/generic-types\">previous\ntutorial</a>, such as , which is a slice of some element\ntype E.</p><p>But we’re not restricted to defined types with names. We can also\nconstruct new types on the fly, using a : that is,\nliterally writing out the type definition as part of the interface.</p><p>For example, this interface specifies a  type\nliteral:</p><pre><code></code></pre><p>A type parameter with this constraint would allow any instance of\nsuch a struct. In other words, its type set contains exactly one type:\n.</p><p>While we can write a generic function constrained by some struct type\nsuch as , there are limitations on what that\nfunction can do with that type. One is that it can’t access the struct’s\n:</p><pre><code></code></pre><p>In other words, we can’t refer to a field on , even\nthough the function’s constraint explicitly says that any \nis guaranteed to be a struct with at least the field .\nThis is a limitation of the Go compiler that has not yet been overcome.\nSorry about that.</p><h2>Some limitations of type\nsets</h2><p>An interface containing type elements can  be used as a\nconstraint on a type parameter. It can’t be used as the type of a\nvariable or parameter declaration, like a basic interface can. That too\nis something that might change in the future, but this is where we are\ntoday.</p><p>What exactly stops us from doing that, though? We already know that\nwe can write functions that take ordinary parameters of some basic\ninterface type such as . So what happens if we try\nto do the same with an interface containing type elements, such as\n?</p><pre><code></code></pre><p>This doesn’t compile, for the reasons we’ve discussed. Some potential\nconfusion arises from the fact that a basic interface can be used as\nboth a regular interface type  a constraint on type\nparameters. But interfaces that contain type elements can only be used\nas constraints.</p><h2>Constraints are not classes</h2><p>If you have some experience with languages that have \n(hierarchies of types), then there’s another thing that might trip you\nup with Go generics: constraints are not classes, and you can’t\ninstantiate a generic function or type on a constraint interface.</p><p>To illustrate, suppose we have some concrete types \nand :</p><pre><code></code></pre><p>And suppose we define some interface  whose type\nset consists of  and :</p><pre><code></code></pre><p>So far, so good, and suppose we now define a generic type\n as a slice of :</p><p>Since we know the type set of  contains exactly\n and , then either of those types\ncan be used to instantiate :</p><pre><code></code></pre><p>What about  itself? Could we create a\n? No, because there’s no such type as\n. It’s a type , not a type, so\nthis gives an error:</p><pre><code></code></pre><p>And, as we’ve seen, we also couldn’t use  as the\ntype of some variable, or ordinary function parameter. Only basic\ninterfaces can be used this way, not interfaces containing type\nelements.</p><p>Well, that’s it for this series. Thanks for sticking around. The\naddition of generics and iterators to Go has opened up a really exciting\nnew world of programming, and my book <a href=\"https://bitfieldconsulting.com/books/generics\">Know\nGo</a> is your complete guide to it. If you’ve enjoyed reading these\nexcerpts, do consider buying the book. And if you’d like to truly master\nthe craft of Go, you can study with me in person, one-to-one, through my\n<a href=\"https://bitfieldconsulting.com/courses/learn-go\">mentoring programme</a>.</p>","contentLength":8752,"flags":null,"enclosureUrl":"","enclosureMime":"","commentsUrl":null}],"tags":["dev"]}